Landing Page -> Login -> Admin Dashboard -> Companies -> Customers -> Document View

Only one admin. No signup, no roles, no public verification route.

Milestones:
1. Landing + Login + Auth
2. Dashboard + Companies CRUD
3. Customers CRUD + Image Upload
4. Document Page + Print

Current core flow:
Admin logs in -> manages companies -> manages customers -> opens printable customer document -> prints when needed.
